# 研究版变更说明：相对 GitHub 基线

> 对比基线：`origin/main`，提交 `5191b537e9416dd95c5ce773bfbe8b6106264c54`。
> 发布目标：跃渊八字分析 v1.8 研究版。

## 原基线是什么

GitHub 基线是 v1.5 时代的纯 Skill 包，主要包含：

- `README.md`、`SKILL.md`；
- `references/` 静态参考库；
- `examples/` 脱敏示例；
- `tests/` 虚构案例与测试结果。

当时 README 仍把项目描述为“不排盘、不做起运计算”，没有纳入当前阶段的计算层、标准命例验收和研究版能力边界。

## 本次发布增加或更新了什么

### 方法论与 Skill

- `SKILL.md` 更新到 **v1.8** 并冻结；
- 纳入 v1.6–v1.8 的研究程序修正：应期信号链、婚恋分层、健康双条件、从格制化闸门、财官反例、应期翻转和职业分化；
- 保留规则层级、证据分级、预注册/后验隔离与 SAFE 红线；
- 增加 `versions/SKILL_v1.8.md` 作为冻结副本。

### 计算与验证

- 增加 `bazi_engine.py`：确定性排盘与基础命理计算层；
- 增加 `mingli_rules.py`：阶段二规则资料层；
- 完成标准命例验收，结果为 **6/6**；原始出生时间与验证脚本仅保留在本地审计工作区，不随公开包发布。

### 执行与交付

- 增加 `references/analysis-checklist.md`：只检查根气归属和透干/暗藏术语，不改变命理规则；
- README 改为 v1.8 研究版定位，说明计算/分析分离、安装方式和能力边界；
- 增加 `docs/`：研究版结项报告、能力边界、真实用户样例摘要和本变更说明。

### 研究结论

- 现版本明确：静态结构判断是相对稳定项；应期与具体事件年份是弱项；
- v3 作为外部诊断封存集，不作为定版准确率依据；
- 未来若修改命理规则，必须新建 v4，不能污染 v3。

## 没有做什么

- 没有把 dev-3/4/5/6 写入 `SKILL.md`；
- 没有放宽婚恋、健康和重大事件 SAFE 红线；
- 没有把 85%/86.2% 写成产品准确率；
- 没有提交真实用户命盘或本地运行时目录。
